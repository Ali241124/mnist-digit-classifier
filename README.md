# mnist-digit-classifier
A deep learning project that uses a Convolutional Neural Network (CNN) to classify handwritten digits (0–9) from the MNIST dataset, deployed as a Flask REST API for real-time inference.
